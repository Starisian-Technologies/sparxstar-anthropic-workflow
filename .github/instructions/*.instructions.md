Note: this `.github/instructions/claude-review.instructions.md` file is not consumed by the current Claude (Anthropic) PR review workflow.
To change platform-wide SPARXSTAR standards enforced by Claude review, update `.github/workflows/claude-pr-review.yml`. `AGENTS.md`, `.github/copilot-instructions.md`, and markdown files matched there (`*.md`, `docs/*.md`, and `specs/*.md`) only provide repo-specific context from the checked-out/caller repository and do not change the central standards.
